[
  {
    "id": "clean-ts",
    "branch": "scenario/clean-ts",
    "sourceTag": "scenario-source/clean-ts",
    "prTitle": "feat: add discount and coupon system",
    "prBody": "Adds a coupon/discount system allowing customers to apply promo codes at checkout. Supports percentage and flat discounts with expiry dates, usage limits, and minimum order amounts.",
    "project": "typescript-fullstack-shop",
    "evaluationFocus": "FALSE POSITIVE TEST. This is intentionally clean, well-written code with zero planted issues. The only job here is to judge whether each tool's comments are warranted. Any flag on this PR is a false positive. Rate tools by how quiet they are — silence is correct behavior.",
    "plantedIssues": []
  },
  {
    "id": "rust-metrics",
    "branch": "scenario/rust-metrics",
    "sourceTag": "scenario-source/rust-metrics",
    "prTitle": "feat: add time-series metrics aggregation library",
    "prBody": "Adds a Rust library for collecting and aggregating time-series metrics. Supports counters, gauges, and histograms with sliding-window aggregation, percentile queries, TTL-based retention, and Prometheus-format text export.",
    "project": "rust-metrics-aggregator",
    "evaluationFocus": "RUST EXPERTISE TEST. Evaluate: (1) Did the tool understand Rust-specific issues (memory ordering, u64 wrapping, integer cast semantics, TOCTOU under RwLock)? (2) Did it catch subtle panics from out-of-bounds indexing? (3) Did it distinguish Rust concurrency correctness from generic threading advice? A tool that misses the atomic ordering bug or the off-by-one in bucket modulo while flagging style issues is underperforming.",
    "plantedIssues": [
      {
        "severity": "blocker",
        "description": "Counter accepts i64 delta but casts to u64 before wrapping_add — a delta of -1 wraps to u64::MAX, corrupting the counter total",
        "category": "data"
      },
      {
        "severity": "blocker",
        "description": "SlidingWindow::bucket_index uses modulo (num_buckets + 1) instead of num_buckets — returns an out-of-bounds index when seq is a multiple of num_buckets, causing a panic",
        "category": "crash"
      },
      {
        "severity": "blocker",
        "description": "Histogram::percentile computes idx = len * p without clamping — panics on out-of-bounds index when p == 1.0",
        "category": "crash"
      },
      {
        "severity": "high",
        "description": "TOCTOU in MetricStore::get_or_create — read lock dropped before write lock acquired without re-checking, allowing concurrent threads to insert duplicate metric IDs for the same name",
        "category": "logic"
      },
      {
        "severity": "high",
        "description": "is_stale uses unchecked u64 subtraction now_ms - last_seen_ms — wraps to huge value on clock skew, incorrectly evicting live metrics",
        "category": "crash"
      },
      {
        "severity": "high",
        "description": "AtomicCounter::snapshot_and_reset uses Ordering::Relaxed for compare_exchange success — reset may not be visible to other threads before they observe the new zero, causing double-counting",
        "category": "logic"
      },
      {
        "severity": "high",
        "description": "to_prometheus() concatenates labels string (which includes braces) with a separate {quantile=...} block for histogram quantile lines — produces double-braces like metric{env=\"prod\"}{quantile=\"0.5\"} which is invalid Prometheus syntax and will be rejected by parsers",
        "category": "data"
      }
    ]
  },
  {
    "id": "huge-ts",
    "branch": "scenario/huge-ts",
    "sourceTag": "scenario-source/huge-ts",
    "prTitle": "feat: add analytics and reporting module",
    "prBody": "Adds a comprehensive analytics and reporting module including sales dashboards, revenue reporting, user segmentation, data export, and metrics aggregation. Supports date range filtering, CSV/JSON export, and real-time dashboard widgets.",
    "project": "typescript-fullstack-shop",
    "evaluationFocus": "PRIORITIZATION UNDER LOAD. This is a 2000+ LOC PR with critical security issues, significant bugs, AND many low-severity smells. The key question: did the tool surface the blockers prominently, or get lost in the noise? A tool that posts 60 comments mostly about style and buries the security issues is failing. Judge both what was found AND how it was presented.",
    "plantedIssues": [
      {
        "severity": "blocker",
        "description": "Code injection: reportType used in eval() in analyticsService — arbitrary code execution"
      },
      {
        "severity": "blocker",
        "description": "Path traversal: filename unsanitized in fs.writeFileSync in exportService"
      },
      {
        "severity": "blocker",
        "description": "Missing auth: admin analytics endpoint has no requireAuth middleware"
      },
      {
        "severity": "blocker",
        "description": "Command injection: user email passed to child_process.exec in reportingService"
      },
      {
        "severity": "blocker",
        "description": "Hardcoded API key in dashboardService"
      },
      {
        "severity": "blocker",
        "description": "Prototype pollution: user JSON merged via Object.assign in analyticsController"
      },
      {
        "severity": "high",
        "description": "Shared report token generated with Math.random() — not cryptographically secure, trivially predictable and brute-forceable"
      },
      {
        "severity": "blocker",
        "description": "Revenue uses string accumulator '0' — string concatenation instead of numeric sum"
      },
      {
        "severity": "blocker",
        "description": "exportAllUserData() has no authorization check — exports all users' PII"
      },
      {
        "severity": "high",
        "description": "Any authenticated user can view any other user's analytics — missing ownership check in analyticsController"
      },
      {
        "severity": "high",
        "description": "calculateGrowthRate divides by previous with no zero guard — NaN/Infinity on first period"
      },
      {
        "severity": "high",
        "description": "Monthly report uses 0-indexed Date.getMonth() vs 1-indexed user input — January never matches"
      },
      {
        "severity": "high",
        "description": "CSV export doesn't escape commas — product names with commas corrupt CSV"
      },
      {
        "severity": "high",
        "description": "getTopProducts sorts by non-existent field — wrong order"
      },
      {
        "severity": "high",
        "description": "normalizeScore returns NaN when min === max"
      },
      {
        "severity": "high",
        "description": "mergeMetrics mutates first argument in-place — callers holding a reference to the original AggregatedMetrics object observe silent data corruption"
      }
    ]
  },
  {
    "id": "spaghetti-python",
    "branch": "scenario/spaghetti-python",
    "sourceTag": "scenario-source/spaghetti-python",
    "prTitle": "feat: add analytics and reporting pipeline",
    "prBody": "Adds analytics and reporting capabilities to the data pipeline. Includes cohort analysis, revenue aggregation, churn calculation, percentile reporting, and multi-format report export.",
    "project": "python-data-pipeline",
    "evaluationFocus": "SIGNAL VS NOISE ON MESSY CODE. The code is deliberately messy with overwhelming smells. The blockers are hidden in the mess. Judge: (1) Did the tool find the real blockers despite the noise? (2) Did it synthesize the quality problem or post 40 individual smell comments? (3) Did it correctly prioritize security over style? A tool that leads with naming conventions while missing pickle deserialization RCE is failing.",
    "plantedIssues": [
      {
        "severity": "blocker",
        "description": "Unsafe pickle deserialization in load_model() — model_path from config, arbitrary code execution"
      },
      {
        "severity": "blocker",
        "description": "SQL injection in get_analytics() — start_date, end_date, dimension all interpolated into query"
      },
      {
        "severity": "blocker",
        "description": "Command injection in export_report() — output_format passed to os.system()"
      },
      {
        "severity": "blocker",
        "description": "Path traversal in generate_report() — report_name written to /var/reports/{report_name}.html"
      },
      {
        "severity": "blocker",
        "description": "Unsafe YAML in load_report_config() — yaml.load with Loader=yaml.Loader"
      },
      {
        "severity": "high",
        "description": "calculate_cohort_retention() uses current period count as cohort size — wrong retention rates"
      },
      {
        "severity": "high",
        "description": "aggregate_revenue_by_period() string-concatenates amounts instead of summing"
      },
      {
        "severity": "high",
        "description": "calculate_churn_rate() denominator fetched before time window filter — inflated"
      },
      {
        "severity": "high",
        "description": "merge_data_sources() silently drops records with None keys"
      },
      {
        "severity": "high",
        "description": "calculate_percentile() sorts copy but indexes original unsorted list"
      },
      {
        "severity": "high",
        "description": "format_currency() rounds then formats using pre-round value"
      },
      {
        "severity": "high",
        "description": "build_comparison_table() divides by previous value with no zero guard"
      }
    ]
  },
  {
    "id": "balanced-java",
    "branch": "scenario/balanced-java",
    "sourceTag": "scenario-source/balanced-java",
    "prTitle": "feat: add subscription and recurring payment support",
    "prBody": "Adds subscription management for recurring payments. Merchants can create payment schedules for customers, and a background processor handles automatic charging on due dates.",
    "project": "java-payment-service",
    "evaluationFocus": "PRECISION BENCHMARK. This looks like a normal, reasonable PR. One blocker is buried in code that looks safe (SQL injection in an 'internal admin search' with a misleading comment). There are 2 real highs and a few smells. Rate based on: did it find the blocker? How many FPs came with it? Did it correctly identify the real highs?",
    "plantedIssues": [
      {
        "severity": "blocker",
        "description": "SQL injection in SubscriptionRepository.searchSubscriptions() — query concatenated into LIKE clause, disguised by comment 'sanitized at controller layer'"
      },
      {
        "severity": "high",
        "description": "createSubscription() has no transaction — subscription and audit log inserted separately, inconsistency if audit fails"
      },
      {
        "severity": "high",
        "description": "updateNextPaymentDate() swallows SQLException cause — re-throws as new RuntimeException(e.getMessage())"
      },
      {
        "severity": "smell",
        "description": "processAllDueSubscriptions() filters in Java instead of SQL WHERE clause"
      },
      {
        "severity": "smell",
        "description": "calculateNextPaymentDate() re-implements LocalDate.plusDays() in 18 lines"
      },
      {
        "severity": "smell",
        "description": "Duplicate validateAmount/validateSubscriptionAmount helpers with unnecessary indirection"
      },
      {
        "severity": "smell",
        "description": "Dead constructor parameter auditLogger never used"
      }
    ]
  }
]
